MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/plastic-swap?retryWrites=true&w=majority
JWT_SECRET=replace_with_a_long_random_string
# Optional: cloudinary or S3 credentials if you move image storage to cloud
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
PORT=5000

# Do NOT commit real secrets. Copy this file to `.env` and fill values before running.
